{
  "name": "trigger-executor-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["trigger-executor"],
  "targets": {
    "e2e": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{e2eProjectRoot}"],
      "options": {
        "jestConfig": "apps/trigger-executor-e2e/jest.config.ts",
        "passWithNoTests": true
      },
      "dependsOn": ["trigger-executor:build", "trigger-executor:serve"]
    }
  }
}
