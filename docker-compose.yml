services:
  postgres:
    image: postgres
    container_name: trigger-auth-db
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: trigger-auth
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - postgres_network

volumes:
  postgres_data:

networks:
  postgres_network:
